<div class="flex flex-col gap-4 p-6 items-start">
  <% sizes.each do |size| %>
    <%= render(Foundation::BaseButtonComponent.new(text: "#{size.to_s.titleize} Button", size: size)) %>
  <% end %>
</div>
