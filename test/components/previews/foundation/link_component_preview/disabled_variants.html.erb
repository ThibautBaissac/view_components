<div class="flex flex-wrap gap-4 p-4">
  <% variants.each do |variant| %>
    <%= render Foundation::LinkComponent.new(
      href: "#",
      text: "#{variant.to_s.titleize} (Disabled)",
      variant: variant,
      disabled: true
    ) %>
  <% end %>
</div>
