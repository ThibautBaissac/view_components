<div class="flex flex-wrap items-center gap-6 p-4">
  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :current, size: :large)) %>
    <span class="text-xs text-gray-500">current</span>
  </div>

  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :primary, size: :large)) %>
    <span class="text-xs text-gray-500">primary</span>
  </div>

  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :secondary, size: :large)) %>
    <span class="text-xs text-gray-500">secondary</span>
  </div>

  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :success, size: :large)) %>
    <span class="text-xs text-gray-500">success</span>
  </div>

  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :warning, size: :large)) %>
    <span class="text-xs text-gray-500">warning</span>
  </div>

  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :danger, size: :large)) %>
    <span class="text-xs text-gray-500">danger</span>
  </div>

  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :info, size: :large)) %>
    <span class="text-xs text-gray-500">info</span>
  </div>

  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :muted, size: :large)) %>
    <span class="text-xs text-gray-500">muted</span>
  </div>

  <div class="flex flex-col items-center gap-2 bg-gray-800 p-2 rounded">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :white, size: :large)) %>
    <span class="text-xs text-white">white</span>
  </div>

  <div class="flex flex-col items-center gap-2">
    <%= render(Foundation::IconComponent.new(name: "check-circle", color: :black, size: :large)) %>
    <span class="text-xs text-gray-500">black</span>
  </div>
</div>
