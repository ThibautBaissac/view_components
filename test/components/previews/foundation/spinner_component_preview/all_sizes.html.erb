<div class="space-y-6 p-6">
  <div class="flex items-center gap-4 border border-slate-200 rounded-lg p-4">
    <span class="text-sm font-medium text-slate-700 w-24">XS:</span>
    <%= render(Foundation::SpinnerComponent.new(size: :xs, label: "Loading")) %>
  </div>

  <div class="flex items-center gap-4 border border-slate-200 rounded-lg p-4">
    <span class="text-sm font-medium text-slate-700 w-24">Small:</span>
    <%= render(Foundation::SpinnerComponent.new(size: :small, label: "Loading")) %>
  </div>

  <div class="flex items-center gap-4 border border-slate-200 rounded-lg p-4">
    <span class="text-sm font-medium text-slate-700 w-24">Medium:</span>
    <%= render(Foundation::SpinnerComponent.new(size: :medium, label: "Loading")) %>
  </div>

  <div class="flex items-center gap-4 border border-slate-200 rounded-lg p-4">
    <span class="text-sm font-medium text-slate-700 w-24">Large:</span>
    <%= render(Foundation::SpinnerComponent.new(size: :large, label: "Loading")) %>
  </div>

  <div class="flex items-center gap-4 border border-slate-200 rounded-lg p-4">
    <span class="text-sm font-medium text-slate-700 w-24">XL:</span>
    <%= render(Foundation::SpinnerComponent.new(size: :xl, label: "Loading")) %>
  </div>
</div>
