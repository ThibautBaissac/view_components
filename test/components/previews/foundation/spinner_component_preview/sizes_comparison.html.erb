<div class="p-6">
  <div class="flex items-end justify-center gap-8">
    <div class="text-center">
      <%= render(Foundation::SpinnerComponent.new(size: :xs, centered: true)) %>
      <p class="mt-2 text-xs text-slate-600">XS</p>
    </div>

    <div class="text-center">
      <%= render(Foundation::SpinnerComponent.new(size: :small, centered: true)) %>
      <p class="mt-2 text-xs text-slate-600">Small</p>
    </div>

    <div class="text-center">
      <%= render(Foundation::SpinnerComponent.new(size: :medium, centered: true)) %>
      <p class="mt-2 text-xs text-slate-600">Medium</p>
    </div>

    <div class="text-center">
      <%= render(Foundation::SpinnerComponent.new(size: :large, centered: true)) %>
      <p class="mt-2 text-xs text-slate-600">Large</p>
    </div>

    <div class="text-center">
      <%= render(Foundation::SpinnerComponent.new(size: :xl, centered: true)) %>
      <p class="mt-2 text-xs text-slate-600">XL</p>
    </div>
  </div>
</div>
